RSpec.describe Jquery::Elycharts::Rails do
  it "has a version number" do
    expect(Jquery::Elycharts::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
